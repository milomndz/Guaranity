var arreglos = require("./arreglos");
module.exports = function sintactico(code, tokens){
    var error = false;
    
    // Hacer logica del sintactico-semantico aqui
    
    if(error){
        errores={
            fallo: error,
            mensajes: ""
        }
    }
    else{
        errores={
            fallo: error,
            mensajes= ""
        }
    }
    return errores;
}